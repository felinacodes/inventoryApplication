<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Director</title>
    <link href="/public/css/tailwind.css" rel="stylesheet">
    <link rel="icon" href="/public/favicon.png" type="image/png">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <script src="/public/js/showPasswordPrompt.js"></script>
    <script src="../public/js/dark-mode-toggle.js"></script>
</head>
<body class="flex flex-col h-full bg-background dark:bg-background-darkBG">
    <%- include('partials/navbar', {links: links}) %>
    <main class="flex-1 flex items-center flex-col p-2 m-2">
        <h1 class="title"> <%= director.first_name %> <%= director.last_name %></h1>
        <div class="w-full max-w-md flex justify-center items-center">
            <img src="<%= director.photo_url ? director.photo_url : '/public/images/default.jpg' %>" alt="director photo"
            class="w-full max-h-90 object-cover rounded-md mb-4 mt-2">
        </div>
        <div class="flex justify-center items-start flex-col dark:text-white mb-4">
            <h2 class="font-normal text-md font-serif mb-2"><span class="font-semibold font-serif">Gender: </span> <%= director.gender? director.gender : 'Unkown' %></h2>
            <h3 class="font-serif mb-2"><span class="font-semibold font-serif">Date of Birth: </span> <%= director.birth_date ? new Date(director.birth_date).toDateString() :'Unkown' %></h3>
            <h3 class="font-serif mb-2"><span class="font-semibold font-serif">Age: </span> <%= director.death_date ? `Died at age ${director.age} on ${new Date(director.death_date).toDateString()}`: director.age%></h3>
        </div>
        
        <div class="w-full justify-center items-center mt-2">
            <%- include('partials/updateDelete', { 
                action: `/directors/${director.id}` 
            }) %>
        </div>
        <h2 class="text-primary font-semibold font-serif">List of movies directed: </h2>
        
        <div class="flex justify-center w-full self-center items-center">    
                <%- include('partials/moviesList', {
                    items: movies,
                    link: '/movies',
                    imgSrc: 'photo_url',
                    title: 'title'
                }) %>                    
        </div>
    </main>
     
    <%- include('partials/footer') %>
</body>
</html>